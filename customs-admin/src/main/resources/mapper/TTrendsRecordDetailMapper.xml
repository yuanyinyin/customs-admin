<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nteport.admin.mapper.TTrendsRecordDetailMapper">

    <resultMap id="BaseResultMap" type="com.nteport.admin.entity.TTrendsRecordDetailEntity">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="headId" column="head_id" jdbcType="BIGINT"/>
            <result property="patrolDate" column="patrol_date" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="creatorName" column="creator_name" jdbcType="VARCHAR"/>
            <result property="createUser" column="create_user" jdbcType="BIGINT"/>
            <result property="updateUser" column="update_user" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,head_id,patrol_date,
        create_time,update_time,creator_name,
        create_user,update_user
    </sql>

    <select id="getDetail" parameterType="long" resultType="hashmap">
        select * from t_trends_record_detail t1
            LEFT JOIN (SELECT id,name from t_point_info )t2
                on t1.point_id = t2.id
        where t1.head_id = #{id,jdbcType=BIGINT}
        ORDER BY t1.create_time asc
    </select>
</mapper>
